a <- 4 #store 4 as variable a
a #display it
a * a #product
a_squared <- a * a
sqrt(a_squared)

v <- c(0, 1, 2, 3, 4) #c stands for concatenate
v #display the vector_valued variable you created

is.vector(v)#check if v is a vector
mean(v)

#operations on vectors
var(v) #variance
median(v) #median
sum(v) #sum all elements
prod(v + 1) # multiply (1 x 2 x 3 x 4 x 5)
length(v) # how many elements in the vector

#variable names and tabbing
wing.width.cm <- 1.2 #using dot notation
wing.length.cm <- c(4.7, 5.2 , 4.8)

wing.length.cm

#types of parentheses
li = list(c(1, 2, 3))
class(li)

